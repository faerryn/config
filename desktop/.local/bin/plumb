#!/bin/sh

MIME="$(file -ib "$(realpath "${1}")")"

case "${MIME}" in
	inode/directory\;*)
		st nnn "${@}"
		;;
	text/*)
		st nvim "${@}"
		;;
	image/*)
		meh "${@}"
		;;
	application/pdf\;*)
		katarakt "${@}"
		;;
	*)
		echo "I don't know how to open ${@}"
		;;
esac
