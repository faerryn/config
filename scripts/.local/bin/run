#!/bin/sh

TERMINAL=st
TUI_PROGRAMS="
bash dash fish sh zsh
git
htop
iwctl
less
man
nvi nvim vi vim
pulsemixer
yes
"

>/dev/null command -v "${1}" || return 1

CMD="$(basename "${1}")"

[ -z "${TERM}" ] && printf "%s\n" ${TUI_PROGRAMS} | grep -qx "${CMD}" &&
		exec $TERMINAL "${@}"

exec "${@}"
