#!/bin/sh

if ! >/dev/null command -V "${1}"; then
	return 1
fi

TERMINAL=st

if [ -z "${TERM}" ]; then
	case "${1}" in
		bash | dash | fish | sh | zsh |\
			htop |\
			less |\
			nvi | nvim | vi | vim)
			exec $TERMINAL "${@}"
			;;
	esac
fi

exec "${@}"
